import 'package:flutter/material.dart';

class UserRegistor extends StatelessWidget {
  const UserRegistor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}